#!/bin/bash
set -e

# Check if pipx is available, if not install via pip with --break-system-packages
if command -v pipx >/dev/null 2>&1; then
    pipx install knowme
else
    pip3 install knowme --break-system-packages
fi

exit 0
